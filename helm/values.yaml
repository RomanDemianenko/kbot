# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: ghcr.io/romandemianenko/kbot
  pullPolicy: IfNotPresent
  tag: v1.0.6-6395caa
  arch: amd64
secret:
  name: "kbot"
  tokenKey: "token"
  tokenName: "TELE_TOKEN"
securityContext:
  privileged: true
