# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: romandemianenko
  tag: "v1.0.5-9770114"

secret:
  name: "kbot"
  tokenKey: "token"
  tokenName: "TELE_TOKEN"

securityContext:
  privileged: true